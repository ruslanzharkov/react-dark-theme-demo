import { palette } from './palette';

export const darkTheme = {
    card: {
        text: palette.light.light1,
        background: palette.dark.dark2,
        boxShadow: palette.boxShadowLight.boxShadow1
    },
    background: palette.dark.dark1,
}